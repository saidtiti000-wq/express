export const isPlugin = Symbol.for('@inversifyjs/plugin/isPlugin');
export class Plugin {
    [isPlugin] = true;
    _container;
    _context;
    constructor(container, context) {
        this._container = container;
        this._context = context;
    }
}
//# sourceMappingURL=Plugin.js.map